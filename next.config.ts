import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode : false // used to prvent double rendering locally [Remove for release]
};

export default nextConfig;
